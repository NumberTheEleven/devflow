# 测试用例清单

> 生成时间: 2026-06-09
> 来源: /devflow:blueprint
> 关联需求: devflow/requirements.md

## TC-001: `/devflow` 启动进入 clarify

**状态:** 已完成
**覆盖:** R-001（`/devflow` 单一入口命令）
**类型:** 手工
**步骤:**
1. 在终端输入 `/devflow 我想加一个用户认证模块`
2. 观察是否进入需求澄清对话
3. 验证无需输入任何子命令名

**预期结果:** 直接进入 clarify 阶段，开始需求对齐对话

---

## TC-002: 原有子命令不再暴露

**状态:** 已完成
**覆盖:** R-001（`/devflow` 单一入口命令）
**类型:** 手工
**步骤:**
1. 尝试输入 `/devflow:implement`
2. 尝试输入 `/devflow:verify`
3. 尝试输入 `/devflow:blueprint`

**预期结果:** 命令不可用，或提示使用 `/devflow`

---

## TC-003: clarify 阶段不产生文件改动

**状态:** 已完成
**覆盖:** R-002（clarify 确认后创建 git worktree）
**类型:** 手工
**步骤:**
1. 运行 `/devflow` 进入 clarify
2. 完成需求澄清对话
3. 检查主仓库 `git status`

**预期结果:** 无任何文件改动，工作区干净

---

## TC-004: clarify 确认后提炼 feature 名称并创建 worktree

**状态:** 已完成
**覆盖:** R-002（clarify 确认后创建 git worktree）
**类型:** 手工
**步骤:**
1. 完成 clarify 对话，需求确认
2. 观察是否从需求中提炼 feature 名称
3. 确认 feature 名称后观察是否创建 worktree

**预期结果:** feature 名称有意义（如 "user-auth"），worktree 创建成功

---

## TC-005: 已在 worktree 中则跳过创建

**状态:** 已完成
**覆盖:** R-002（clarify 确认后创建 git worktree）
**类型:** 手工
**步骤:**
1. 在已有 worktree 中运行 `/devflow`
2. 观察是否会再次创建 worktree

**预期结果:** 检测到已在 worktree 中，跳过创建，直接恢复流程

---

## TC-006: EnterWorktree 不可用时降级

**状态:** 已完成
**覆盖:** R-002（clarify 确认后创建 git worktree）
**类型:** 手工
**步骤:**
1. 模拟 EnterWorktree 不可用的环境
2. 运行 `/devflow` 完成 clarify

**预期结果:** 提示无法创建隔离环境，在主目录继续后续流程

---

## TC-007: 阶段完成后自动进入下一阶段

**状态:** 已完成
**覆盖:** R-003（阶段自动推进 + checkpoint）
**类型:** 手工
**步骤:**
1. 完成 clarify 阶段（用户确认需求）
2. 观察是否自动进入 breakdown 阶段

**预期结果:** 无需手动调用，自动推进

---

## TC-008: checkpoint 默认"Y 继续"

**状态:** 已完成
**覆盖:** R-003（阶段自动推进 + checkpoint）
**类型:** 手工
**步骤:**
1. 阶段完成出现 checkpoint 提示
2. 直接回复 "Y" 或回车

**预期结果:** 进入下一阶段

---

## TC-009: checkpoint 跳转到指定阶段

**状态:** 已完成
**覆盖:** R-003（阶段自动推进 + checkpoint）
**类型:** 手工
**步骤:**
1. clarify 完成后 checkpoint 出现
2. 回复 "跳转到 implement"

**预期结果:** 跳过 breakdown 和 blueprint，直接进入 implement

---

## TC-010: 用户不回复时自然暂停

**状态:** 已完成
**覆盖:** R-003（阶段自动推进 + checkpoint）
**类型:** 手工
**步骤:**
1. checkpoint 出现后不回复
2. 关闭会话
3. 之后重新在 worktree 中运行 `/devflow`

**预期结果:** 从上次暂停的阶段恢复，不重复已完成阶段

---

## TC-011: verify 全部通过后自动清理 worktree

**状态:** 已完成
**覆盖:** R-004（流程完成自动清理 worktree）
**类型:** 手工
**步骤:**
1. 完成所有 5 个阶段，verify 全部通过
2. 观察清理流程是否触发

**预期结果:** worktree 目录被删除，关联分支被移除

---

## TC-012: 分支合并到主分支

**状态:** 已完成
**覆盖:** R-004（流程完成自动清理 worktree）
**类型:** 手工
**步骤:**
1. 清理流程触发后
2. 切换到主分支检查提交历史

**预期结果:** worktree 分支的提交已合并到主分支

---

## TC-013: 列出所有未完成 worktree

**状态:** 已完成
**覆盖:** R-005（暂停 worktree 列表）
**类型:** 手工
**步骤:**
1. 创建 2-3 个 worktree 并在不同阶段暂停
2. 运行列表命令

**预期结果:** 显示每个 worktree 的 feature 名称、当前阶段、创建时间

---

## TC-014: 清理指定未完成 worktree

**状态:** 已完成
**覆盖:** R-006（手动清理未完成 worktree）
**类型:** 手工
**步骤:**
1. 选择一个暂停的 worktree
2. 执行清理操作

**预期结果:** worktree 目录被删除，分支被移除

---

## TC-015: 清理前确认

**状态:** 已完成
**覆盖:** R-006（手动清理未完成 worktree）
**类型:** 手工
**步骤:**
1. 执行清理操作
2. 观察是否弹出确认提示

**预期结果:** 显示 worktree 详情，要求确认后才执行删除

---

## TC-016: worktree 内持久化当前阶段

**状态:** 已完成
**覆盖:** R-007（Worktree 状态追踪）
**类型:** 手工
**步骤:**
1. 在 breakdown 阶段暂停
2. 检查 worktree 内 `devflow/` 目录

**预期结果:** 存在状态文件，记录当前阶段为 "breakdown"

---

## TC-017: 重新进入时恢复到暂停阶段

**状态:** 已完成
**覆盖:** R-007（Worktree 状态追踪）
**类型:** 手工
**步骤:**
1. 在 breakdown 阶段暂停后退出
2. 重新运行 `/devflow`

**预期结果:** 自动跳过 clarify，从 breakdown 继续

---

## TC-018: 各阶段模块可独立调用

**状态:** 已完成
**覆盖:** R-008（渐进过渡 — 保留内部阶段能力）
**类型:** 单元
**步骤:**
1. 直接调用 clarify 内部模块
2. 直接调用 breakdown 内部模块
3. 验证各模块输入输出接口

**预期结果:** 每个阶段模块可独立运行，通过 `devflow/` 文件传递数据

---

## TC-019: 所有模板字段为中文

**状态:** 已完成
**覆盖:** R-009（模板与输出语言统一为中文）
**类型:** 手工
**步骤:**
1. 检查 requirements.md 模板字段名
2. 检查 design.md 模板字段名
3. 检查 tasks.md 模板字段名
4. 检查 test-cases.md 模板字段名

**预期结果:** 所有字段名均为中文（优先级、状态、描述、依赖、验收标准等）

---

## TC-020: 状态值为中文

**状态:** 已完成
**覆盖:** R-009（模板与输出语言统一为中文）
**类型:** 手工
**步骤:**
1. 运行完整流程
2. 检查各阶段生成的状态文件

**预期结果:** 状态值显示为"待开始 / 进行中 / 已完成"

---

## TC-021: 运行时输出为中文

**状态:** 已完成
**覆盖:** R-009（模板与输出语言统一为中文）
**类型:** 手工
**步骤:**
1. 触发 checkpoint 提示
2. 触发一个错误场景（如 worktree 创建失败）
3. 检查输出语言

**预期结果:** 所有面向用户的运行时输出均为中文

---

*由 DevFlow 追踪。请勿手动编辑。*
